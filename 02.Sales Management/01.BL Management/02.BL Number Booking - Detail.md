### 📃페이지명 : BLBookingDetailForm

### AS-IS

![[Pasted image 20260826175720.png|491]]

### TO-BE

![[Pasted image 20260826175818.png]]

### 관련 페이지

- BLScnSearchPopupForm : Shipper (퍼블로 인해 미개발)

* CommonCodePopupForm : Maker

### 오류 사항 
	- v

### 소스 관련

- form_OnUnloadCompleted 처리
  - before

```javascript
    this.form_OnUnloadCompleted = function(obj:nexacro.Form, e:nexacro.CloseEventInfo) {

        (async () => {
            // 수정여부 판단후 저장할것인지 물어본다.
            if(await com.isUpdateDataset(this.Detail_SBLBookingInfoDS)) {
                var result = await com.fnMessageOpenForm(this, "Q", "Domain.msg.common.VAL_0019");
                if (result == 'YES') {
                    this.isExit = true;
                    await this.fnFormSave();
        			this.fnFormClose("TRUE");
                    //return false;
                } else if (result == 'CANCEL') {
        			this.fnFormClose("FALSE");
                    //return false;
                }
            }

        }).call(this);

    };

```

- after

```javascript
this.fnClose = function () {
  (async () => {
    // 수정여부 판단후 저장할것인지 물어본다.
    if (await com.isUpdateDataset(this.Detail_SBLBookingInfoDS)) {
      var result = await com.fnMessageOpenForm(
        this,
        "Q",
        "Domain.msg.common.VAL_0019",
      );
      if (result == "YES") {
        this.isExit = true;
        await this.fnFormSave();
        this.fnFormClose("TRUE");
        //return false;
      } else if (result == "CANCEL") {
        this.fnFormClose("FALSE");
        //return false;
      }
    }
  }).call(this);
};
```
